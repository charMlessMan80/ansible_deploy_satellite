---
# Qualification (pre-production / staging) inventory for Red Hat Satellite 6.19
# Usage: ansible-playbook -i inventories/qualification/hosts.yml site.yml
all:
  children:
    satellite:
      hosts:
        satellite.qual.example.com:
          ansible_host: 10.20.0.10
